# Tooltip

用于 当既要保持 UI 简洁，同时又需要对不熟悉的人解释 的时候，会使用 `Tooltips` 组件进行内容提示。

## Playground

<code src="./playground.tsx" />

## API

继承自 [Popover](../popover/). 移除了 `floatingArrowProps` 和 `offsetHeight` 属性.
